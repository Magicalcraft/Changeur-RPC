{
  "name": "rpc-changeur",
  "version": "1.0.0",
  "description": "Changer RPC discord",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron main.js",
    "rebuild": "./node_modules/.bin/electron-rebuild",
    "mac-packager": "electron-packager . --overwrite --platform=darwin --arch=x64 --prune=true --out=release-builds",
    "win-packager": "electron-packager . Changeur-RPC --overwrite --asar=true --platform=win32 --arch=ia32  --prune=true --out=release-builds --version-string.CompanyName=GertrudeDev --version-string.FileDescription=CE --version-string.ProductName='ChangeurRPC'"
  },
  "author": "Gertrude dev",
  "license": "ISC",
  "devDependencies": {
    "electron": "^1.8.8",
    "electron-packager": "^13.1.0",
    "electron-rebuild": "^1.8.4"
  },
  "dependencies": {
    "asar": "^1.0.0",
    "discord-rpc": "^3.0.1",
    "electron": "^1.8.8",
    "electron-rebuild": "^1.8.4"
  }
}
